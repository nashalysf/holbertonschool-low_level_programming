#include "main.c"
#include <stdio.h>
/**
*_atoi- Entry point
*@s: string param
*Description: converts a string
*to an integer
*Return: 0. (Success)
*/
int _atoi(char *s)
{
int i = 1;
int x = 0;
while(s)


return (0);
}
